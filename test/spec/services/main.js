'use strict';

describe('Service: UAParser', function () {

  // load the controller's module
  beforeEach(module('angularuaparser'));

  var UAParser;

  // Initialize the controller and a mock scope
  beforeEach(inject(function (_UAParser_) {
    UAParser = _UAParser_;
  }));

});
